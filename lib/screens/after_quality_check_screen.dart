import 'package:flutter/material.dart';

class AfterQualityCheckScreen extends StatelessWidget {
  const AfterQualityCheckScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
